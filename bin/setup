#!/bin/bash
cd "$(dirname $0)/.." || { echo 'What!?'; exit 1; }

. bin/utils.sh

assert_commands lein npm

npm install \
  && echo_success "Installed npm dependencies" \
  || echo_failure "Failed to install npm dependencies"
